services:
  web:
    image: partenaires-web-dev
    build:
      context: .
      target: deps
    working_dir: /app
    command: npm start
    ports:
      - "${PORT_WEB:-3000}:3000"
    environment:
      - NODE_ENV=prod
    volumes:
      - .:/app
      - /app/node_modules
    container_name: ${COMPOSE_PROJECT_NAME:-site-club-tesla}-site